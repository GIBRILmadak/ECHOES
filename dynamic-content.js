document.addEventListener('DOMContentLoaded', () => {
    const feed = document.getElementById('feed');

    if (feed) {
        // Charger le fil
        // Supprimé pour éviter le conflit avec posts.js
    }
});